name: CI

env:
  NODE_VERSION: 26

on:
  push:
    branches:
      - '**'
  pull_request:

permissions:
  contents: read

jobs:
  frontend:
    name: Build frontend bundle
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build:frontend

  desktop-release:
    name: Build desktop release artifact
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v4.5.0
        with:
          versionSpec: 6.7.x

      - name: Determine version
        id: version
        uses: gittools/actions/gitversion/execute@v4.5.0

      - name: Stamp desktop version
        shell: pwsh
        run: |
          $version = '${{ steps.version.outputs.semVer }}'
          $buildInstallers = '${{ github.event_name }}' -eq 'push' -and '${{ github.ref_name }}' -eq 'main'
          $bundleTargets = if ($buildInstallers) { @('msi', 'nsis') } else { @('app') }
          $configPath = 'tauri/tauri.conf.json'
          $config = Get-Content $configPath -Raw | ConvertFrom-Json
          $config.version = $version
          $config.bundle.targets = $bundleTargets
          $config | ConvertTo-Json -Depth 10 | Set-Content $configPath -Encoding utf8

      - name: Build desktop bundle
        run: npm run build

      - name: Stage desktop release files
        shell: pwsh
        run: |
          $version = '${{ steps.version.outputs.semVer }}'
          $buildInstallers = '${{ github.event_name }}' -eq 'push' -and '${{ github.ref_name }}' -eq 'main'
          $stageRoot = "artifacts\desktop-release"
          New-Item -ItemType Directory -Force -Path "$stageRoot\exe" | Out-Null
          New-Item -ItemType Directory -Force -Path "$stageRoot\meta" | Out-Null
          Copy-Item "tauri\target\release\mudshow.exe" "$stageRoot\exe\mudshow-$version.exe" -Force
          if ($buildInstallers) {
            New-Item -ItemType Directory -Force -Path "$stageRoot\msi" | Out-Null
            New-Item -ItemType Directory -Force -Path "$stageRoot\nsis" | Out-Null
            Copy-Item "tauri\target\release\bundle\msi\*.msi" "$stageRoot\msi" -Force
            Copy-Item "tauri\target\release\bundle\nsis\*.exe" "$stageRoot\nsis" -Force
          }
          Set-Content "$stageRoot\meta\version.txt" $version -Encoding utf8

      - name: Upload raw exe artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop-exe
          path: artifacts/desktop-release/exe/mudshow-*.exe
          if-no-files-found: error

      - name: Upload MSI artifact
        if: ${{ github.event_name == 'push' && github.ref_name == 'main' }}
        uses: actions/upload-artifact@v4
        with:
          name: desktop-msi
          path: artifacts/desktop-release/msi/*.msi
          if-no-files-found: error

      - name: Upload NSIS artifact
        if: ${{ github.event_name == 'push' && github.ref_name == 'main' }}
        uses: actions/upload-artifact@v4
        with:
          name: desktop-nsis
          path: artifacts/desktop-release/nsis/*.exe
          if-no-files-found: error

      - name: Upload version artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop-version
          path: artifacts/desktop-release/meta/version.txt
          if-no-files-found: error
