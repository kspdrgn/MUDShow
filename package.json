{
  "name": "mudshow",
  "version": "1.0.0",
  "description": "MUDShow!",
  "main": "proxy.js",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "start": "npm run build && npm run proxy",
    "build": "npm i && npm run clean && tsc --build --verbose && npm run copystatic",
    "copystatic": "copyfiles --flat src/mudshow.html src/JetBrainsMono.ttf dist",
    "proxy": "node dist/proxy/proxy.js httpPort=9000",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.20.1"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "copyfiles": "^2.4.1",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}
